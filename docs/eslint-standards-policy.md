# ESLint 代码标准策略

## 🎯 双重标准策略

本项目采用**双重标准策略**，对生产代码和测试代码实施不同的ESLint标准：

### 📋 **策略概述**

| 文件类型     | 标准级别     | 适用范围                                | 核心原则             |
| ------------ | ------------ | --------------------------------------- | -------------------- |
| **生产代码** | **严格标准** | `src/**/*.{ts,tsx}` (排除测试文件)      | 企业级质量，零容忍   |
| **测试代码** | **宽松标准** | `**/*.test.{ts,tsx}`, `**/__tests__/**` | 效率优先，安全不妥协 |

## 🏭 **生产代码：严格标准**

### ⚠️ **绝不妥协的规则**

```javascript
{
  // 复杂度控制 (企业级标准)
  'complexity': ['error', 10],                    // 函数复杂度 ≤ 10
  'max-lines-per-function': ['error', 80],        // 函数长度 ≤ 80行
  'max-params': ['error', 5],                     // 参数数量 ≤ 5个
  'max-nested-callbacks': ['error', 3],           // 嵌套层数 ≤ 3层
  'max-lines': ['error', 500],                    // 文件长度 ≤ 500行
  'max-statements': ['error', 30],                // 语句数量 ≤ 30个

  // 代码质量 (零容忍)
  'no-console': 'error',                          // 禁止console语句
  '@typescript-eslint/no-explicit-any': 'error', // 禁止any类型
  'no-unused-vars': 'error',                     // 禁止未使用变量

  // 安全标准 (绝不妥协)
  'security/detect-object-injection': 'error',
  'security/detect-non-literal-fs-filename': 'error',
  'security-node/detect-insecure-randomness': 'error',
}
```

### 🎯 **生产代码质量目标**

- **函数复杂度：** ≤ 10 (强制)
- **函数长度：** ≤ 80行 (强制)
- **类型安全：** 100% (禁止any)
- **安全漏洞：** 0个 (零容忍)

## 🧪 **测试代码：宽松标准**

### ✅ **放宽的结构性规则**

```javascript
{
  // 测试文件特殊需求
  'max-lines-per-function': ['error', 800],       // 测试套件可以更长
  'complexity': ['error', 20],                    // 测试逻辑可以更复杂
  'max-nested-callbacks': ['error', 6],           // describe/it嵌套需要
  'max-lines': ['error', 1000],                   // 完整测试文件
  'max-statements': ['error', 60],                // 测试步骤较多
  'max-params': ['error', 8],                     // 测试函数参数较多

  // 测试常见模式
  'no-magic-numbers': ['error', {
    ignore: [0, 1, -1, 100, 200, 404, 500],       // 允许HTTP状态码等
    ignoreArrayIndexes: true,
    ignoreDefaultValues: true,
  }],
  'prefer-arrow-callback': 'off',                 // 测试中function更清晰
  'no-unused-expressions': 'off',                 // expect().toBe()等断言
  'no-empty-function': 'off',                     // 空的mock函数
}
```

### ⚠️ **微调优化的规则**

```javascript
{
  // 从Error改为Warning - 允许但不鼓励
  '@typescript-eslint/no-explicit-any': 'warn',   // 测试中的mock对象
  'require-await': 'warn',                        // 测试中的async模式
  'no-new': 'warn',                               // 测试中的mock对象创建
}
```

### 🔒 **仍然严格的安全规则**

```javascript
{
  // 安全规则绝不放宽
  'security/detect-object-injection': 'error',
  'security/detect-non-literal-fs-filename': 'error',
  'security/detect-unsafe-regex': 'error',
  'security-node/detect-insecure-randomness': 'error',
  'no-console': 'error',                          // 避免测试日志污染
  'no-undef': 'error',                            // 基本语法错误
}
```

## 📊 **策略效果**

### ✅ **成功指标**

- **生产代码错误：** 19个 → 目标：0个
- **测试代码错误：** 54个 → 大部分为Warning
- **开发效率：** 节省20-40小时修复时间
- **代码质量：** 生产代码保持企业级标准

### 🎯 **质量保证**

- **生产部署：** 只有生产代码影响，测试代码不打包
- **安全标准：** 所有文件都保持严格的安全规则
- **类型安全：** 生产代码100%类型安全，测试代码警告提醒

## 🚨 **重要提醒**

### ❌ **绝对禁止**

- **生产代码采用宽松标准** - 这会影响产品质量
- **放宽安全相关规则** - 安全无小事
- **忽略Warning** - Warning是改进提醒

### ✅ **推荐做法**

- **代码审查时关注测试代码质量**
- **定期重构过于复杂的测试文件**
- **优先修复生产代码的所有Error**
- **逐步改进测试代码的Warning**

## 📝 **配置文件位置**

- **主配置：** `eslint.config.mjs`
- **生产代码规则：** `code-quality-config` 部分
- **测试代码规则：** `jest-config` 部分

---

**最后更新：** 2025-08-02  
**策略版本：** v1.0  
**负责人：** 开发团队
